name: Test version latest keyword
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          version: 'latest'
          # No credentials — action will fail on auth, but 'latest' version keyword should be accepted
      - name: Assert action ran with latest version keyword
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action should fail due to missing credentials, not due to the 'latest' version keyword.
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed as expected (credentials missing, 'latest' version keyword accepted)"
          elif [ "$OUTCOME" = "success" ]; then
            echo "FAIL: action unexpectedly succeeded without credentials"
            exit 1
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
